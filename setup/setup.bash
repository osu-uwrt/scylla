# Development is only officially supported on linux. For things like changing program stuff, you can probably install stuff manually on Windows, but if you're building the app, it will NOT work. 

# RUN THIS SCRIPT FROM THE 'SETUP' DIRECTORY OR ELSE EVERYTHING WILL BE WRONG 
bash setupPythonPrereqs.bash 
bash setupJS.bash
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
config.js
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# Default npm / electron .gitignore stuff 
node_modules
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OpenLabeling user-controlled files not necessary to compile / run 
src/OpenLabeling/main/input/*
src/OpenLabeling/main/output/*

# Development requisites that we don't want to bloat the repo with that are installed as part of setup 
src/site_packages
src/ZipFiles/*
